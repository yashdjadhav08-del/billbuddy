# Dependencies
node_modules/

# Build output
dist/
target/
*.wasm

# Environment (commit only .env.example)
.env
.env.local
.env.*.local

# Mock shared-state runtime artifact (persisted per-device; recreated on boot)
mock-state.json

# Editor / OS
.DS_Store
Thumbs.db
*.log

.vercel
